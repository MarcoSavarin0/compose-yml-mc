services:
  mc:
    image: itzg/minecraft-server
    environment:
      EULA: true
      TYPE: PAPER
    ports:
      - "25566:25565"
    volumes:
      - mc-data:/data
      - ./config:/config:ro

volumes:
  mc-data: {}